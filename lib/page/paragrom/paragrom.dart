import 'package:flutter/material.dart';

class ParagromPage extends StatelessWidget {
  const ParagromPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('工作台')
    );
  }
}